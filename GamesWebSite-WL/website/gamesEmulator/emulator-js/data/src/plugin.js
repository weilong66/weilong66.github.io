/* $(document).ready(function() {
    const $ejsCanvas = $('.ejs_canvas');
    let $resolution = $(".ejs_settings_transition");
    let $resolutionDiv = $resolution.parent('div');
    console.log($ejsCanvas)
    console.log($resolution)
    $resolutionDiv.find('button').on('click', function() {
        console.log('直接子级按钮被点击:', $(this).attr('value'));

        $ejsCanvas.attr('width',$(this).attr('value'));
    });;
}); */